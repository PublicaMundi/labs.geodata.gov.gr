Install ansible (preferably from your package manager):
http://docs.ansible.com/intro_installation.html

For running ansible playbook:

Add server IPs under /etc/ansible/hosts

Then from terminal:
ansible-playbook --private-key=~/.ssh/id_rsa.tzotsos-ipsy main.yml
